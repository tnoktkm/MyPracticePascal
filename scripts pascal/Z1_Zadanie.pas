
var a,b,c,d,e :integer;
begin
readln(a);
readln(b);
readln(c);
readln(d);
readln(e);

if a mod 2=1 then
writeln(('Не чётное ') + a)
else
writeln(('Чётное ') + a);

if b mod 2=1 then
writeln(('Не чётное ') + b)
else
writeln(('Чётное ') + b);

if c mod 2=1 then
writeln(('Не чётное ') + c)
else
writeln(('Чётное ') + c);

if d mod 2=1 then
writeln(('Не чётное ') +d)
else
writeln(('Чётное ') + d);

if e mod 2=1 then
writeln(('Не чётное ') + e)
else
writeln(('Чётное ') + e);4
end.

